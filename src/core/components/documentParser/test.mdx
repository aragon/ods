import { Button } from '../button/button.tsx';
import { IconType } from '../icon/iconType.ts';

# Welcome to Our Documentation

This MDX file demonstrates the rendering capabilities of the **CopyParser** component.

## Features

This a list with tiered sublists:

-   Markdown syntax for **bold**, _italic_, and <del>strikethrough</del> text.
-   Lists, both ordered and unordered:
    1. First item
    2. Second item
    -   Bullet item
    -   Another bullet item

### JSX Components

You can embed JSX directly:

<Button variant="critical" className="not-prose" iconLeft={IconType.PLUS} onClick={() => alert('MDX is live!')}>
    Click me
</Button>

### Code Blocks

Here's a simple code snippet:

```javascript
useEffect(() => {
    const processContent = async () => {
        if (children && typeof children === 'string') {
            let html: string;

            if (children.includes('<') && children.includes('>')) {
                html = DOMPurify.sanitize(children);
            } else {
                const markdownHtml = await marked(children);
                html = DOMPurify.sanitize(markdownHtml);
            }

            setProcessedContent(html);
        }
    };

    processContent();
}, [children]);
```

<hr />

# Heading 1

## Heading 2

### Heading 3

#### Heading 4

##### Heading 5

###### Heading 6

<hr />

<blockquote>
    This is a blockquote.

    WITH MULTIPLE LINES!

    All your base are belong to us.

</blockquote>
