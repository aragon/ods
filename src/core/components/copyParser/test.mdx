# Welcome to Our Documentation

This MDX file demonstrates the rendering capabilities of the **CopyParser** component.

## Features

-   Markdown syntax for **bold**, _italic_, and ~~strikethrough~~ text.
-   Lists, both ordered and unordered:
    1. First item
    2. Second item
    -   Bullet item
    -   Another bullet item

### JSX Components

You can embed JSX directly:

<button
    className="rounded-xl border border-primary-900 bg-primary-100 px-2"
    onClick={() => alert('You clicked the button!')}
>
    Click Me!
</button>

### Code Blocks

Here's a simple code snippet:

```javascript
console.log('Hello, world!');
```
